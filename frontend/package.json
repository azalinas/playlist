{
  "name": "playlist-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.tsx --bundle --servedir=dist --outfile=dist/bundle.js --define:process.env.NODE_ENV=\"development\" --watch",
    "build": "esbuild src/main.tsx --bundle --outfile=dist/bundle.js --minify --define:process.env.NODE_ENV=\"production\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.5",
    "typescript": "^5.4.3"
  }
}
